#include <stdio.h>
/**
 * main -point
 *
 * return 0 (success)
 */
int main(void)
{
	char chaine[] = "_putchar"
		for (int i = 0; chaine[i] != '\0'; i++)
		{
			putchar(chaine[i]);
		}
		putchar('\n');
	return (0);
}
